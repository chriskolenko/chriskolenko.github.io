{
    "scripts": {
        "build:pdf": "resume export resume.pdf --theme short",
        "build:html": "resume export resume.html --theme short",
        "serve": "resume serve --theme short"
    },
    "devDependencies": {
        "resume-cli": "^3.0.5"
    },
    "dependencies": {
        "jsonresume-theme-elegant": "^1.16.1",
        "jsonresume-theme-flat": "^0.3.7",
        "jsonresume-theme-kendall": "^0.2.0",
        "jsonresume-theme-short": "^0.1.7"
    }
}
