<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, user-scalable=no, minimal-ui">
</head>
<body>
  <main>
    {{> components/resume-header }}
    <div class="flex">
      <aside class="flex-initial">
        {{> components/about }}
        {{> components/skills }}
        {{> components/languages }}
        {{> components/interests }}
      </aside>
      <div class="flex-1">
        {{> components/summary}}
        {{> components/work }}
        {{> components/volunteer }}
        {{> components/education }}
        {{> components/awards }}
        {{> components/publications }}
        {{> components/references }}
      </div>
    </div>
  </main>
</body>
</html>